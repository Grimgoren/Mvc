{% extends "base.html.twig" %}
{% block body %}
<h1>All Available Cards</h1>
<ul>
    {% for card in cards %}
    <div class = card-container>
        <div class = allcards>
            <p>{{ card.getAsString() }}</p>
        </div>
    </div>
    {% endfor %}
</ul>
{% endblock %}
