{% extends "base.html.twig" %}
{% block body %}
<h1>Drawn Card</h1>
<ul>
    <p>Remaining cards: {{ deckCount }}</p>
    {% for card in cards %}
    <div class = card-container>
        <div class = allcards>
            <p>{{ card.getAsString }}</p>
        </div>
    </div>
    {% endfor %}
</ul>
{% endblock %}
